# ADR-003: Deny-by-default tool access

Unknown tools and missing permissions are denied. Tool risk, role, tenant and side-effect classification are explicit metadata.

Critical operations require a human approval boundary even when a role technically permits the operation.
