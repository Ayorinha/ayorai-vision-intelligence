# Architecture Decision Records

See the `adr/` directory for the decisions that define the safety boundary.

The invariant is: retrieval is untrusted, model output is advisory, policy is deterministic, tools are least-privilege, and consequential actions require human approval.
